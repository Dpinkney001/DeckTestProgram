/// testing the creation of monster cards

// enum monster types.

enum monsterType {
	1 == FIEND,
	2 == SPELLCASTER,
	3 == ROCK,
	4 == MACHINE,
	5 == ZOMBIE,
	6 == DRAGON,
	7 == 



	// enum monster attribute

	enum monsterAttribute {
		1 == DARK,
		2 == LIGHT,
		3 == WIND,
		4 == EARTH,
		5 == FIRE,
		6 == WATER,
		7 == DIVINE,

 int Dark_Necrofear(){
	int stars = 8;
	int atk = 2200;
	int def = 2800;
	int monsterType = FIEND;
	int monsterAttribute = DARK;
	int effectMonster = true;
	int normalMonster = false;
	// effect

	if ( graveyardFiendCount => 3 ){
